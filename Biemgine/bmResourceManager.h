#pragma once
#include <map>
#include <string>
#include "bmShader.h"
using namespace std;

class bmResourceManager
{
public:

	void loadTexture(const string & name, const string& vspath, const string& fspath);
};

